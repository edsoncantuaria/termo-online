# Copiar para /root/termo-online/.env na VM (opcional; systemd já define o essencial)
PORT=8001
TERM0_API_ONLY=1
TERM0_RELOAD=0
TERM0_DATA=/root/termo-online/data
TERM0_CORS_ORIGINS=https://termo.cloudive.com.br
TERM0_LOG_LEVEL=INFO
